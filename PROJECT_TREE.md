Отличная идея! Да, можно создать скрипт, который автоматически обновляет дерево проекта. Создадим два файла:

## 📁 **1. Создаём файл проекта (PROJECT_TREE.md)**

```bash
cat > PROJECT_TREE.md << 'EOF'
# 🌳 Дерево проекта DreamCalc

*Автоматически обновляемый обзор структуры проекта*

```
DreamCalc/
├── 📄 index.html                    # Главный HTML (PWA точка входа)
├── 📄 manifest.json                 # PWA манифест
├── 📄 service-worker.js             # Service Worker для оффлайн-работы
├── 📄 offline.html                  # Страница оффлайн-режима
├── 📄 README.md                     # Описание проекта
├── 📄 ROADMAP.md                    # План развития
├── 📄 PROJECT_TREE.md               # Этот файл (дерево проекта)
│
├── 📁 styles/                       # Стили
│   └── 📄 main.css                  # Основные стили (Bootstrap + кастом)
│
├── 📁 scripts/                      # JavaScript модули
│   ├── 📄 app.js                    # Главный модуль приложения
│   ├── 📄 calculator.js             # Бизнес-логика расчётов
│   ├── 📄 uiComponents.js           # UI компоненты
│   ├── 📄 dreamData.js              # Данные целей
│   ├── 📄 utils.js                  # Утилиты и хелперы
│   ├── 📄 storage.js                # LocalStorage менеджер
│   └── 📄 charts.js                 # Chart.js графики
│
├── 📁 docs/                         # Документация
│   ├── 📄 ARCHITECTURE.md           # Архитектурные решения
│   ├── 📄 DECISIONS.md              # Принятые решения
│   └── 📄 LEARNINGS.md              # Извлечённые уроки
│
├── 📁 icons/                        # Иконки PWA
│   ├── 📄 README.txt                # Описание папки иконок
│   └── 📄 ...                       # Иконки разных размеров
│
├── 📁 screenshots/                  # Скриншоты
│   └── 📄 README.txt                # Описание папки скриншотов
│
├── 📁 assets/                       # Статические файлы
│   └── 📄 ...                       # Дополнительные ресурсы
│
└── 📁 .git/                         # Git репозиторий
    └── 📄 ...                       # Git конфигурация
```

## 📊 Статистика проекта

**Последнее обновление:** $(date)

**Количество файлов по типам:**
- HTML: $(find . -name "*.html" -not -path "./.git/*" | wc -l)
- JavaScript: $(find . -name "*.js" -not -path "./.git/*" | wc -l)
- CSS: $(find . -name "*.css" -not -path "./.git/*" | wc -l)
- Markdown: $(find . -name "*.md" -not -path "./.git/*" | wc -l)
- JSON: $(find . -name "*.json" -not -path "./.git/*" | wc -l)
- Всего файлов: $(find . -type f -not -path "./.git/*" | wc -l)

**Размер проекта:** $(du -sh . | cut -f1)

